import { useEffect, useState } from "react";
import { Link } from "react-router-dom";

function Home() {  }
    // const [reviews, setReviews] = useState({});
    // const [gym_locations, setGymLocations] = useState({});

    // useEffect(() => {
    // fetch("/reviews")
    //     .then((r) => r.json())
    //     .then(setReviews);
    // }, []);
    
    // useEffect(() => {
    // fetch("/gym_locations")
    //     .then((r) => r.json())
    //     .then(setGymLocations);
    // }, []);}

export default Home;